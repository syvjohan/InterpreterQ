#include <stdio.h>
#include <stdlib.h>

// Stack, Heap, global.
//Efter new kontrollera om det är en null pointer då är det bad alloc.

#define MAXSIZE 30

size_t sp = 0;
int stk[MAXSIZE];


void pop() {
	if (sp > 0) {

	}
}

void push(char *str) {

}